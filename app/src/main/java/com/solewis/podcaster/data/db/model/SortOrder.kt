package com.solewis.podcaster.data.db.model

/** Per-show episode list ordering, persisted on [com.solewis.podcaster.data.db.entity.PodcastEntity]. */
enum class SortOrder {
    NEWEST_FIRST,
    OLDEST_FIRST
}
